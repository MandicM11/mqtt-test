{
  "MqttSettings": {
    "Broker": "localhost",
    "Port": 1883,
    "Username": "test",
    "Password": "test123",
    "Topic": "Test/mqtt",
    "Role": "Publisher", // Can be Publisher or Subscriber
    //"PublishedFilePath": "publishing.txt", //name of the file/data ur publishing(not needed if you are doing DB to DB comunication directly)
    "SavedFilePath": "./",
    "PublisherConnectionString": "Host=localhost;Database=Mqtt;Username=postgres;Password=1234", //DB connection string that is publishing the data
    "SubscriberConnectionString": "Host=localhost;Database=Mqtt1;Username=postgres;Password=1234" //DB connection string that is recieving the data
  }
}
